node_modules/
dist/
src-tauri/target/

# Regenerated by tauri-build on every compile.
src-tauri/gen/schemas/

# Working notes: written against a real machine, so they carry real workspace
# paths, prompt text and spend figures. The distilled version lives in README.
PLAN.md

*.local
.DS_Store
